import { CSSProperties } from 'styled-components';

const ButtonDefaultStyles: { [key: string]: CSSProperties } = {
  disabledStyles: {
    backgroundColor: '#D4E3FC',
    color: 'white',
  },
};

export default ButtonDefaultStyles;
